package mainapp;
import statisticsUI.StatisticsUI;


public class MainAppStatisticsUI {
	/**
	 * Main Method to execute the Statistics as a seperate program
	 * @param args
	 */
    public static void main(String[]args){
    	String host = "localhost";
		if(args.length > 0)
			host = args[0];
		
    	final StatisticsUI staticticsUI = new StatisticsUI(host,"9000","9001");
    	staticticsUI.setVisible(true);

    }
}
