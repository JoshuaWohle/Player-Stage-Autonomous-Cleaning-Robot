package consumerUI;

import utilities.Session;

/**
 * This interface is used make sure that any classes that implement ScheduleListener
 * implements these methods too 
 * @author Team Cyan
 *
 */
public interface ScheduleListener {

	
	public void newSession(Session cleaningSession);
	public void removeSession(Session removedSession);
	
	
}
